<UserControl x:Class="HW1.Views.TaskManagerPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md ="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HW1.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="DimGray" >
        
        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
            
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Column="0" Background="SlateGray">
            
            <Grid.RowDefinitions>
                
                <RowDefinition Height="0.5*"/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Task Manager" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White">
                <TextBlock.Effect>
                    
                    <DropShadowEffect/>
                    
                </TextBlock.Effect>
            </TextBlock>
            
            <Button Grid.Row="1" Content="🛠️Processes" FontSize="19" Background="Transparent" Foreground="White" Margin="20" Command="{Binding ProcessesComand}" >
                <Button.Effect>
                    <DropShadowEffect/>
                </Button.Effect>
            </Button>
            
            <Button Grid.Row="2" Content="🚫Blacklist" FontSize="19" Background="Transparent" Foreground="White" Margin="20,0,20,60" Command="{Binding BlackListCommand}">
                <Button.Effect>
                    <DropShadowEffect/>
                </Button.Effect>
            </Button>
            
        </Grid>
        
        <Grid Grid.Column="1">
            
            <Grid.RowDefinitions>
                
                <RowDefinition Height="0.13*"/>
                <RowDefinition/>
                
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="SlateGray">
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    
                    <Button  Width="50" Height="50" Background="Transparent" BorderBrush="Transparent" Content="X" Command="{Binding CloseAppCommand}"/>
                    
                </StackPanel>
                
            </Grid>
            
            <Grid Row="1">
                
                <UserControl Content="{Binding CurrentView, UpdateSourceTrigger=PropertyChanged}"/>
                
            </Grid>
            
        </Grid>
        
    </Grid>
</UserControl>
